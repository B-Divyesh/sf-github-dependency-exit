# Polish round 5 — all findings closed

Repair scope: release candidate `9dd092c6c82c70b805411a46600997dd5fe2f936`, using every record in `.factory/review-*.md` and `.factory/polish-*.md`. Product repair is commit `d9514447f1887f46909d49d245134f508102d379`; the independent clean-clone claim log is commit `0a70879deddfa721f6cbd1e44eaac086a1c77190`.

## Finding map

| Finding | Change made | Evidence |
| --- | --- | --- |
| F-1-1 — route focus | History navigation moves focus to the destination H1 and announces the route. | `npm test` — `Back and Forward focus the heading for the restored route`; [`live-qa.json`](evidence/polish-5/live-qa.json) records `backFocus` and `forwardFocus` as `true`; live [`/?demo=1`](https://github-dependency-exit.sociobot.in/?demo=1). |
| F-1-2 — route metadata | Home, demo, legal pages, and 404 retain route-specific titles, descriptions, canonical URLs, Open Graph, and Twitter metadata. | `npm test` route-metadata checks; [`live-qa.json`](evidence/polish-5/live-qa.json) records each rendered title, description, and canonical; live [`/privacy`](https://github-dependency-exit.sociobot.in/privacy). |
| F-1-3 — real 404 | The designed 404 includes the shared shell, legal links, metadata, and home action while the response remains HTTP 404. | `npm test` — `the static 404 has complete metadata and the standard site shell`; [`live-404-headers.txt`](evidence/polish-5/live-404-headers.txt); live [missing route](https://github-dependency-exit.sociobot.in/polish-5-missing). |
| F-1-4, reopened as F-4-1 — unclear report wording | Generated reports use `Inventory totals`; retired “exit surface” and “accumulated load” wording is rejected by regression coverage. | `npm test` report-wording regression and `npm test -- --grep @claim:cli-demo-temp-dir`; live release-binary SHA check in handoff evidence. |
| F-3-1 — paid claims lacked outcome proof | One-price, owner-wide-report, refund, checkout, and browser license-storage behavior remain separately claimed and fixture-tested. | `npm test -- --grep @claim:paid-scope`, `@claim:paid-owner-scan`, `@claim:refund-revokes-license`, `@claim:dodo-hosted-checkout`, and `@claim:browser-license-storage`; live checkout in [`live-links.json`](evidence/polish-5/live-links.json). |
| F-3-2 — “Exit survey” terminology | Product, report, demo, and README consistently call this a GitHub dependency inventory. | `npm test` — first-screen terminology regression; [`copy-audit.md`](copy-audit.md); live [home](https://github-dependency-exit.sociobot.in/). |
| F-4-2a — demo output location | The default CLI demo creates a temporary report directory and prints it. | `npm test -- --grep @claim:cli-demo-temp-dir`; all-claim clean-clone log [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt). |
| F-4-2b — browser/CLI fixture parity | Browser JSON download and CLI demo derive from the same bundled inventory fixture. | `npm test -- --grep @claim:demo-fixture-parity`; [`demo-390.png`](evidence/polish-5/demo-390.png); live [`/?demo=1`](https://github-dependency-exit.sociobot.in/?demo=1). |
| F-4-2c — license storage/removal | The browser stores, verifies, and removes a pasted license locally. | `npm test -- --grep @claim:browser-license-storage`; clean-clone log [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt). |
| F-4-2d — Rust 1.85 support | The stated Rust 1.85 floor remains source-compatible and the test command now provisions that exact toolchain. | `npm run test:msrv -- --grep @claim:rust-1-85-build`; absent-toolchain proof [`msrv-provisioning.txt`](evidence/polish-5/msrv-provisioning.txt). |
| F-4-2e — release/download identity | The site build stages the release binary and the claim compares its SHA-256 with the download. | `npm test -- --grep @claim:binary-download-build-match`; live download SHA-256 `505dc109e146a2965f09868d6a4f8d95239decdfb94b95d4d01d51456ed018c4`. |
| F-4-2f — GHES API base | `--api-base` is covered against a GitHub Enterprise Server 3.14 `/api/v3` fixture. | `npm test -- --grep @claim:ghes-api-base`; clean-clone log [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt). |
| F-5-1 — clean MSRV command failed | Added `scripts/ensure-rust-toolchain.mjs`, made `npm test` preflight it, and changed the manifest to the complete `npm run test:msrv` command. | Exact clean-clone absent-toolchain run in [`msrv-provisioning.txt`](evidence/polish-5/msrv-provisioning.txt), then `1 passed`; all 30 claims pass in [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt). |
| F-5-2 — breakage headline overclaimed | Rewrote the H1 to “Map GitHub dependencies before you move,” aligned title/description, and constrained headline wrapping so the action fits the first desktop and mobile screen. | `npm test` — first-screen metadata/layout regressions; [`home-390.png`](evidence/polish-5/home-390.png), [`home-1440.png`](evidence/polish-5/home-1440.png); live [home](https://github-dependency-exit.sociobot.in/). |
| F-5-3 — private token/permission claims absent | Added `private-token-auth` plus a private API fixture that rejects no token, asserts the `GITHUB_TOKEN` Bearer header, and confirms a report. Copy now says to grant only required read permissions and marks unreadable checks unknown. | `npm test -- --grep @claim:private-token-auth`; all-claim clean-clone log [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt); live [home](https://github-dependency-exit.sociobot.in/). |
| F-5-4 — OAuth limitation absent from claims | Added `oauth-manual-review`; JSON and Markdown fixtures assert the exact app/OAuth unknown and installation-settings next step. Copy now states the CLI limitation rather than an external API promise. | `npm test -- --grep @claim:oauth-manual-review`; all-claim clean-clone log [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt); live [demo](https://github-dependency-exit.sociobot.in/?demo=1). |
| F-5-5 — unsupported “risk points rank” promise | Removed the aggregate score, its unsupported ranking copy, report table column, fixture values, demo panel, and types. Regression coverage rejects the retired language. | `npm test` — review-score/wording regression; `npm run typecheck`; clean-clone `npm test` pass in [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt). |
| F-5-6 — generic “THE PRODUCT” label | Replaced the decorative label with “Sample migration report.” | `npm test` first-screen copy check; [`home-390.png`](evidence/polish-5/home-390.png); live [home](https://github-dependency-exit.sociobot.in/). |

## Earlier unnumbered findings

The older reviews also recorded normal `- uses:` workflow parsing, denied-metadata unknowns, pagination, rate-limit stopping, JSON-only stdout, token exclusion, sourced evidence, documented alternatives, Sociobot metadata isolation, no-network CLI demo, offline shell refresh, mobile reflow/touch targets, checkout redirect, lint/format checks, and download links. They remain observable through their dedicated manifest commands and passed together from a clean clone; see [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt), [`live-links.json`](evidence/polish-5/live-links.json), and [`live-qa.json`](evidence/polish-5/live-qa.json).

## Final verification

- A fresh no-local clone at `d9514447f1887f46909d49d245134f508102d379` ran all 30 exact `.factory/claims.json` commands independently: PASS. The committed transcript is [`clean-clone-claims.txt`](evidence/polish-5/clean-clone-claims.txt).
- In that clean clone, `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, and `cargo package --allow-dirty` passed. `dist/site` was produced.
- The controller-specific MSRV proof removed Rust 1.85.0 first, then the exact claim command reinstalled it automatically and passed. See [`msrv-provisioning.txt`](evidence/polish-5/msrv-provisioning.txt).
- Local `verify-url.sh` passed on home and demo; its title/lang/one-H1/main/alt/console records are under `evidence/polish-5/verify-local-*`.
- Mobile Lighthouse: Performance 100, Accessibility 100, Best Practices 100, SEO 100; FCP 0.9 s, LCP 1.8 s, TBT 20 ms, CLS 0. See [`lighthouse-mobile.json`](evidence/polish-5/lighthouse-mobile.json).
- Static deployment `ddea4c34-0c1e-4049-8b7a-326543238ee5` was cold-checked at the production URL. Home, demo, Privacy, Terms, and 404 have no serious/critical axe issues, no unexpected console errors, no horizontal overflow, correct focus, and an offline demo reload. See [`live-qa.json`](evidence/polish-5/live-qa.json) and the live route captures.

No blocking, minor, or historical finding remains open.
