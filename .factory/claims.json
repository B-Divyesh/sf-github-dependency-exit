[
  {
    "id": "sample-demo",
    "claim": "Try it with sample data at ?demo=1 in one click",
    "where": "landing primary action",
    "test": "npm test -- --grep @claim:sample-demo",
    "sandbox": "fresh browser context; click the landing action; assert the populated demo and banner"
  },
  {
    "id": "cli-demo",
    "claim": "The CLI demo writes JSON and Markdown without setup",
    "where": "landing terminal, README",
    "test": "npm test -- --grep \"@claim:cli-demo(?:\\s|$)\"",
    "sandbox": "fresh temporary directory; bundled sample data; no token"
  },
  {
    "id": "demo-privacy",
    "claim": "The browser demo sends no data off site and saves no browser data",
    "where": "landing facts, demo banner, privacy page",
    "test": "npm test -- --grep @claim:demo-privacy",
    "sandbox": "fresh browser context; capture every request while using demo filters; assert local and session storage stay empty"
  },
  {
    "id": "read-only-api",
    "claim": "Inventory uses read-only GitHub requests",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:read-only-api",
    "sandbox": "local fake GitHub API; run a repository scan and assert every request method is GET"
  },
  {
    "id": "json-export",
    "claim": "Downloads the sample inventory as JSON",
    "where": "demo report",
    "test": "npm test -- --grep @claim:json-export",
    "sandbox": "fresh browser context; download JSON and assert report counts"
  },
  {
    "id": "paid-scope",
    "claim": "$39 once for owner-wide scans; one-repository scans stay free",
    "where": "landing facts and price section, README",
    "test": "npm test -- --grep @claim:paid-scope",
    "sandbox": "fresh browser context; assert the price, free scope, and Sociobot checkout URL"
  },
  {
    "id": "public-no-token",
    "claim": "Public repository scans work without a token",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:public-no-token",
    "sandbox": "local public repository API fixture; run with no token and assert a report is written"
  },
  {
    "id": "local-reports",
    "claim": "Reports stay in the output folder",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:local-reports",
    "sandbox": "fresh temporary directory; assert JSON and Markdown are written there"
  },
  {
    "id": "no-migration",
    "claim": "The CLI does not change repository data",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:no-migration",
    "sandbox": "local fake GitHub API; assert every request is GET and the repository fixture stays unchanged"
  },
  {
    "id": "workflow-step-syntax",
    "claim": "Finds normal GitHub Actions step references",
    "where": "README",
    "test": "npm test -- --grep @claim:workflow-step-syntax",
    "sandbox": "local API fixture; normal '- uses:' lines including a commit-pinned action"
  },
  {
    "id": "unknown-access",
    "claim": "Unreadable GitHub metadata becomes unknown checklist work",
    "where": "landing preview, README",
    "test": "npm test -- --grep @claim:unknown-access",
    "sandbox": "private-repository fixture; denied workflow content, package access, and branch 404"
  },
  {
    "id": "paginated-inventory",
    "claim": "Checks every page of GitHub list results",
    "where": "README",
    "test": "npm test -- --grep @claim:paginated-inventory",
    "sandbox": "local workflow fixture with 101 items over two pages"
  },
  {
    "id": "rate-limit-stop",
    "claim": "Stops when GitHub reports a rate limit",
    "where": "README",
    "test": "npm test -- --grep @claim:rate-limit-stop",
    "sandbox": "local API fixture; first follow-up request returns 403 with x-ratelimit-remaining: 0"
  },
  {
    "id": "script-json",
    "claim": "JSON mode writes one parseable inventory to stdout",
    "where": "README",
    "test": "npm test -- --grep @claim:script-json",
    "sandbox": "run bundled demo with --json; JSON.parse stdout and assert progress is stderr"
  },
  {
    "id": "token-not-reported",
    "claim": "The token is never written to a report",
    "where": "README",
    "test": "npm test -- --grep @claim:token-not-reported",
    "sandbox": "local API fixture; scan with a known token and assert inventory.json excludes it"
  },
  {
    "id": "sourced-evidence",
    "claim": "Every checked area keeps its source",
    "where": "landing product preview",
    "test": "npm test -- --grep @claim:sourced-evidence",
    "sandbox": "local representative API fixture; assert source records for successful, empty, denied, and unknown checks"
  },
  {
    "id": "documented-alternatives",
    "claim": "Alternatives are verified only when target-forge documentation is linked",
    "where": "README evidence guide",
    "test": "npm test -- --grep @claim:documented-alternatives",
    "sandbox": "local scan fixture; assert a verified documented alternative and an unsupported unknown alternative"
  },
  {
    "id": "sociobot-metadata-privacy",
    "claim": "Repository metadata is not sent to Sociobot",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:sociobot-metadata-privacy",
    "sandbox": "local GitHub and license fixtures; capture a full owner scan and assert Sociobot receives only the license"
  },
  {
    "id": "cli-demo-no-network",
    "claim": "The CLI demo does not call GitHub or Sociobot",
    "where": "landing terminal and .factory/demo.md",
    "test": "npm test -- --grep @claim:cli-demo-no-network",
    "sandbox": "fresh temporary directory with all standard HTTP proxies pointed to a rejecting local tripwire; assert zero requests and both reports"
  }
]
